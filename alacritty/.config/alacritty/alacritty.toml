# Load base config first, then local overrides
# Alacritty loads imports in order, so the LAST file wins.
general.import = [
    "~/.config/alacritty/base.toml",
    "~/.config/alacritty/local.toml"
]
